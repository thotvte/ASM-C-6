@model IEnumerable<web_ban_do_an.Models.Category>
<div class="col-lg-3">
    <div class="hero__categories">
        <div class="hero__categories__all">
            <i class="fa fa-bars"></i>
            <span>All departments</span>
        </div>
        <ul>
            @foreach(var item in Model)
            {
                <li><a href="#">@item.Name</a></li>
            }
            
        </ul>
    </div>
</div>